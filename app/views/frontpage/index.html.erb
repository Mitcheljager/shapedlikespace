<%= inline_svg "layout/background-blobs-header-2.svg", class: "header__blobs" %>

<section class="featured">
  <h1><center>Featured Creations</center></h1>

  <div class="featured__items">
    <% @featured_posts.each do |post| %>
      <%= link_to post_path(post.slug), class: "featured__item" do %>
        <div class="featured__item-content">
          <% if post.images.any? %>
            <%= image_tag post.images.first.variant(quality: 95, resize_to_fill: [370, 370]).processed, class: "featured__image" %>
          <% else %>
            <%= image_tag "https://placehold.it/370x370", class: "featured__image" %>
          <% end %>
        </div>
      <% end %>
    <% end %>
  </div>
</section>

<h1><center>Popular Creations</center></h1>

<%= render "posts/posts", posts: @hot_posts %>

<center><%= link_to "View more", posts_path, class: "button button--primary button--inline top-offset" %></center>

<section class="categories">
  <% categories.each do |category| %>
    <div class="category">
      <%= category %>
    </div>
  <% end %>

  <%= inline_svg "layout/background-blobs-content.svg", class: "categories__blob" %>
</section>

<h1><center>Recent Creations</center></h1>

<%= render "posts/posts", posts: @posts %>
